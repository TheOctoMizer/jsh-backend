{
  "name": "jsh-backend-ts",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "start": "bun src/index.ts",
    "build": "tsc -p . && bunx build --out-dir dist src/index.ts",
    "dev": "bun --watch src/index.ts"
  }
}